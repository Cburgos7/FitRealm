---
phase: 02-core-movement-loop
plan: 05
type: execute
wave: 3
depends_on: [02-01, 02-04]
files_modified:
  - supabase/migrations/20260604020000_phase2_decay_cron.sql
  - apps/mobile/components/village/GraceBadge.tsx
  - apps/mobile/hooks/useVillage.ts
  - apps/mobile/app/(tabs)/village/index.tsx
autonomous: false
requirements: [VLG-06, VLG-07, VLG-08, VLG-09]

must_haves:
  truths:
    - "Food decays −2.5 per 6-hour tick (rate from game_config) via Supabase pg_cron — server-only, never client-side"
    - "Villages still inside the 24h grace window do not decay; a 'Protected — Xh left' badge shows the countdown"
    - "After each tick, food_state is recomputed (Thriving/Hungry/Starving) so the village UI reflects decay on next app open"
    - "Even a maxed village still loses food each tick (balance invariant); decay floors at 0"
    - "Decay is shown on app-open as a narrative drain, not as an interruption"
  artifacts:
    - path: "supabase/migrations/20260604020000_phase2_decay_cron.sql"
      provides: "decay_village_food() Postgres function + pg_cron 6h schedule"
      contains: "decay_village_food"
    - path: "apps/mobile/components/village/GraceBadge.tsx"
      provides: "'Protected — Xh left' countdown badge"
      min_lines: 10
  key_links:
    - from: "supabase/migrations/20260604020000_phase2_decay_cron.sql"
      to: "game_config"
      via: "decay function reads food_decay_per_tick + food_hungry_threshold"
      pattern: "food_decay_per_tick"
    - from: "supabase/migrations/20260604020000_phase2_decay_cron.sql"
      to: "pg_cron"
      via: "cron.schedule('food-decay-6h', '0 */6 * * *', ...)"
      pattern: "cron.schedule"
    - from: "apps/mobile/components/village/GraceBadge.tsx"
      to: "villages.grace_expires_at"
      via: "countdown from grace_expires_at"
      pattern: "grace_expires_at|graceExpiresAt"
---

<objective>
Build the server-only food decay engine: a Postgres decay_village_food() function scheduled every 6 hours via Supabase pg_cron (NOT Vercel Cron — Hobby plan caps cron at once/day per RESEARCH). It reads the decay rate from game_config, skips villages still in their 24h grace window, floors food at 0, and recomputes food_state. Plus the client-side grace countdown badge and app-open narrative drain framing.

Purpose: Decay is the heartbeat that makes movement essential — without it the loop has no stakes. It must be server-authoritative and ungameable.
Output: decay_village_food() + pg_cron schedule applied to hosted Supabase, GraceBadge, app-open drain framing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-movement-loop/02-RESEARCH.md
@.planning/phases/02-core-movement-loop/02-VALIDATION.md
@.planning/phases/02-core-movement-loop/02-01-SUMMARY.md
@.planning/phases/02-core-movement-loop/02-04-SUMMARY.md

<interfaces>
From Plan A (02-01): villages.food (numeric), food_state, grace_expires_at (default now()+24h), last_decay_at. game_config keys: food_decay_per_tick (2.5), food_decay_cadence_h (6), food_hungry_threshold (20), grace_period_hours (24). useVillage exists.
From Plan D (02-04): the food_state CASE recompute pattern is already in allocate_food — mirror it in the decay function for consistency.

RESEARCH to use directly: Pattern 6 (CREATE EXTENSION pg_cron; decay_village_food() reading game_config; cron.schedule('food-decay-6h','0 */6 * * *', ...)); Pitfall 1 (Vercel Hobby cron = once/day, why pg_cron); Assumption A3 (pg_cron enabled on free tier — verify; fallback is a client-triggered RPC on app open). CLAUDE.md invariants: decay server-only (VLG-06), balance invariant — maxed village still decays (VLG-08).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: decay_village_food() function + pg_cron schedule + [BLOCKING] push</name>
  <read_first>
    - .planning/phases/02-core-movement-loop/02-RESEARCH.md (Pattern 6 decay function + schedule; Pitfall 1; Assumption A3 fallback)
    - supabase/migrations/20260604000000_phase2_game.sql (Plan A schema + game_config seed — must be pushed)
    - supabase/migrations/20260604010000_phase2_allocate_rpc.sql (Plan D food_state CASE to mirror)
    - .planning/phases/02-core-movement-loop/02-01-SUMMARY.md + 02-04-SUMMARY.md (confirm prior pushes succeeded)
  </read_first>
  <action>
    Create supabase/migrations/20260604020000_phase2_decay_cron.sql: CREATE EXTENSION IF NOT EXISTS pg_cron; define public.decay_village_food() RETURNS void, SECURITY DEFINER, per RESEARCH Pattern 6 — read v_decay_rate from game_config key 'food_decay_per_tick' and v_hungry_threshold from 'food_hungry_threshold' (no hardcoded 2.5/20, INFRA-02); UPDATE public.villages SET food = GREATEST(0, food - v_decay_rate), food_state = CASE WHEN GREATEST(0, food - v_decay_rate)=0 THEN 'starving' WHEN GREATEST(0, food - v_decay_rate) <= v_hungry_threshold THEN 'hungry' ELSE 'thriving' END, last_decay_at = now(), updated_at = now() WHERE grace_expires_at < now() (grace skip, VLG-06; balance invariant — every non-grace village loses food, VLG-08). Add a header comment marking the forward-compatible building-modifier read point (VLG-07 Watchtower decay reduction in Phase 6) and that VLG-09 raiders are a distinct future threat — do NOT implement buildings or raiders. Schedule: SELECT cron.schedule('food-decay-6h', '0 */6 * * *', 'SELECT public.decay_village_food()'). Do NOT build a Vercel cron endpoint (Pitfall 1 / Open Question 2). Then push from repo root: `npx supabase db push` (applies 20260604020000). If pg_cron is not enabled (A3), the migration's CREATE EXTENSION should enable it; if the platform blocks it, document the fallback (client-triggered RPC on app open) in the SUMMARY but keep pg_cron as the primary. This is a [BLOCKING] push — autonomous:false (interactive Supabase auth possible on Windows; SUPABASE_ACCESS_TOKEN for non-interactive).
  </action>
  <verify>
    <automated>grep -c "decay_village_food\|cron.schedule\|food_decay_per_tick\|GREATEST(0" supabase/migrations/20260604020000_phase2_decay_cron.sql</automated>
  </verify>
  <acceptance_criteria>
    - Migration defines decay_village_food() reading food_decay_per_tick + food_hungry_threshold from game_config, flooring food at 0, recomputing food_state, and skipping villages where grace_expires_at >= now().
    - cron.schedule('food-decay-6h', '0 */6 * * *', ...) present; no Vercel cron endpoint added.
    - Pushed to hosted Supabase (confirm `select * from cron.job where jobname='food-decay-6h'` returns a row, or document the A3 fallback in the SUMMARY).
    - VLG-07/VLG-08/VLG-09 captured as forward-compatible comments (buildings/raiders not implemented).
  </acceptance_criteria>
  <done>Server-only 6h food decay live via pg_cron; rate from game_config; grace respected; balance invariant held.</done>
</task>

<task type="auto">
  <name>Task 2: Grace countdown badge + app-open decay drain framing</name>
  <read_first>
    - apps/mobile/app/(tabs)/village/index.tsx (Village screen to add the badge into)
    - apps/mobile/hooks/useVillage.ts (village fetch incl. grace_expires_at, last_decay_at)
    - apps/mobile/components/village/FoodMeter.tsx (Plan A — drain animation hook)
    - .planning/phases/02-core-movement-loop/02-CONTEXT.md (D2-25 grace badge copy; D2-28 drain-on-app-open; D2-33 state-transition toast)
  </read_first>
  <action>
    Create apps/mobile/components/village/GraceBadge.tsx — shows "Protected — Xh left" computed from villages.grace_expires_at vs now (hide when expired), with reassuring first-time copy (D2-25/D2-41). Wire it into village/index.tsx near the Food meter. In useVillage (or the village screen), detect a food drop since the last seen snapshot (compare last_decay_at / food delta on refetch) and surface the decay as a narrative drain on the FoodMeter + a warm toast on app-open (e.g., "Thornhaven grows hungry" on a Thriving→Hungry transition) — NOT a modal/interruption (D2-28/D2-33). Ensure the client NEVER subtracts food on a timer — it only reads server food (VLG-06; no client decay). Refetch the village on app foreground so the post-cron food/food_state is reflected.
  </action>
  <verify>
    <automated>cd apps/mobile && npm test && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - GraceBadge computes the countdown from grace_expires_at and hides once expired.
    - The village screen refetches on foreground and frames a food drop as a narrative drain + toast, not a modal.
    - No client-side timer mutates food anywhere (grep shows no setInterval/decrement of food in client code) (VLG-06).
    - `npm test` green; `tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Grace countdown badge + app-open decay drain framing wired; decay stays server-authoritative.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify decay tick + grace + state transitions (manual SQL + device)</name>
  <read_first>
    - .planning/phases/02-core-movement-loop/02-VALIDATION.md (## Manual-Only — E2E decay; VLG-06 manual SQL test)
  </read_first>
  <what-built>The pg_cron decay function, grace skip, food_state recompute, grace badge, and app-open drain framing are implemented. pg_cron timing + the full decay→starve→recover loop need SQL + device verification.</what-built>
  <how-to-verify>
    1. In the Supabase SQL editor, manually invoke: `SELECT public.decay_village_food();` on a village past its grace window → confirm food dropped by exactly food_decay_per_tick (2.5) and food_state recomputed (VLG-06).
    2. On a village still inside grace (grace_expires_at > now) → confirm decay_village_food() did NOT change its food (grace respected). Confirm the app shows "Protected — Xh left" (D2-25).
    3. Confirm the schedule exists: `select jobname, schedule from cron.job where jobname='food-decay-6h'` → '0 */6 * * *'.
    4. Repeatedly invoke decay until food reaches 0 → confirm food floors at 0 and food_state='starving'; the app shows the dark/desaturated starving scene + "Your village awaits your return" card (VLG-04 from Plan A).
    5. Full loop: from starving, Hunt Food (Plan D) → village unlocks (VLG-05); then on next app open after a real or manual tick, confirm the drain toast framing (D2-28/D2-33).
    6. Balance invariant: set a village to food=100 and tick → confirm it still loses 2.5 (VLG-08).
  </how-to-verify>
  <resume-signal>Type "verified" or describe issues (esp. if pg_cron is not schedulable — record the A3 fallback decision).</resume-signal>
  <acceptance_criteria>Human confirms server-only decay decrements correctly, respects grace, floors at 0, recomputes state, and the maxed village still decays — completing the move→bank→allocate→decay loop.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pg_cron → villages (internal) | Runs inside Postgres; no HTTP surface |
| client → villages.food | Client reads only; must never write decay |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02E-CLI | Tampering | client-side decay bypass/gaming | mitigate | Decay runs only in decay_village_food() via pg_cron; client never mutates food on a timer (VLG-06, CLAUDE.md invariant) |
| T-02E-DOS | Denial of Service | cron decay endpoint abuse | mitigate | pg_cron runs internally; no HTTP decay endpoint exposed (Pitfall 1 / Security Domain) |
| T-02E-CFG | Tampering | altered decay rate from client | mitigate | Rate read from game_config inside the function; game_config SELECT-only RLS (Plan A) |
| T-02E-BAL | Tampering | bypass balance invariant | mitigate | Decay applies to all non-grace villages with no zero-decay path; floors at 0 only, never skips (VLG-08) |
</threat_model>

<verification>
- `grep` confirms decay_village_food reads game_config, floors at 0, recomputes state, skips grace, and schedules via cron.schedule.
- `cd apps/mobile && npm test` + `tsc --noEmit` clean.
- Manual SQL + device E2E (Task 3) confirms VLG-06 decrement, grace skip, starving floor, balance invariant (VLG-08), and the full loop.
</verification>

<success_criteria>
- decay_village_food() decrements food by the game_config rate every 6h via pg_cron (server-only), skips grace-window villages, floors at 0, and recomputes food_state.
- Grace countdown badge + app-open narrative drain framing on the client; no client-side decay anywhere.
- Maxed village still decays (balance invariant). Full core loop (move→bank→allocate→decay→starve→recover) verified end-to-end.
- VLG-07/VLG-08/VLG-09 captured as forward-compatible comments (buildings/raiders deferred to later phases).
</success_criteria>

<output>
Create `.planning/phases/02-core-movement-loop/02-05-SUMMARY.md` when done.
</output>
